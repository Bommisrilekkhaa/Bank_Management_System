<div class="transactions-list">
  <button {{action "addNewTransaction"}} class="new-transaction-btn">New Transaction</button>

  <div class="filters">
    <label for="transactionType">Type</label>
    <select id="transactionType" value={{selectedTransactionType}} onchange={{action (mut selectedTransactionType) value="target.value"}}>
      <option value="">All</option>
      <option value="emi">EMI</option>
      <option value="debit">Debit</option>
      <option value="credit">Credit</option>
    </select>

    <label for="transactionStatus">Status</label>
    <select id="transactionStatus"  value={{selectedTransactionStatus}}  onchange={{action (mut selectedTransactionStatus) value="target.value"}}>
      <option value="">All</option>
      <option value="success">Success</option>
      <option value="pending">Pending</option>
    </select>
    
    <button class="clear" {{action (mut selectedTransactionType) ''}} {{action (mut selectedTransactionStatus) ''}} >Reset</button>
  </div>

  <table class="transactions-table">
    <thead>
      <tr>
        <th>Transaction ID</th>
        <th>Transaction Type</th>
        <th>Account Number</th>
        <th>Amount</th>
        <th>Date and Time</th>
      </tr>
    </thead>
    <tbody>
      {{#each filteredTransactions as |transaction|}}
        <tr {{action "viewTransaction" transaction}}>
           <td>{{transaction.transaction_id}}</td>
          <td>{{transaction.transaction_type}}</td>
          <td>{{transaction.acc_number}}</td>
          <td>Rs.{{transaction.transaction_amount}}</td>
          <td>{{transaction.transaction_datetime}}</td>
        </tr>
      {{/each}}
    </tbody>
  </table>
</div>
