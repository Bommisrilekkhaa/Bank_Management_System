<div class="branches-list">
  <button {{action "addNewBranch"}} class="new-branch-btn">New Branch</button>
  
  <table class="branches-table">
    <thead>
      <tr>
        <th>Branch ID</th>
        <th>Branch Name</th>
        <th>Branch Number</th>
        <th>Actions</th>
      </tr>
    </thead>
    <tbody>
      {{#each branches as |branch|}}
        <tr >
          <td {{action "viewBranch" branch}}>{{branch.branch_id}}</td>
          <td {{action "viewBranch" branch}}>{{branch.branch_name}}</td>
          <td {{action "viewBranch" branch}}>{{branch.branch_number}}</td>
          <td>
            <button {{action "editBranch" branch}}>Edit</button>
            {{#unless (eq branch.branch_id branch.main_branch_id)}}
            <button {{action "deleteBranch" branch}}>Delete</button>
            {{/unless}}
          </td>
        </tr>
      {{/each}}
    </tbody>
  </table>
</div>
