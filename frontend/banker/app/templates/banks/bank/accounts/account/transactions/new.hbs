{{transaction-input
  accNo=this.accNo
  bankId=this.bankId
  isDirect=false
  transaction_type=this.transaction_type
  transaction_amount=this.transaction_amount
  toTransaction=(action "toTransaction")
}}
