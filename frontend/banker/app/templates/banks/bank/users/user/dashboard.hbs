{{#if (eq sessionData.user_role "ADMIN")}}
<div class="card-container">
  {{#each branches as |branch|}}
   {{admin-dashboard
      branchName=branch.branchName
      accountCount=branch.accountCount
      loansAvailed=branch.loansAvailed
      totalDeposits=branch.totalDeposits
   }}
  {{/each}}
</div>
{{/if}}

{{#if (eq sessionData.user_role "MANAGER")}}
    {{manager-dashboard
    branch=branch}}

{{/if}}
{{#if (eq sessionData.user_role "CUSTOMER")}}
    {{customer-dashboard
    accountList = accountList}}

{{/if}}

