{{ view-transactions
    accNo=this.accNo
    transactions=this.transactions
    isDirect=true
    totalTransactions= this.totalTransactions
    changeTransactions=(action "changeTransactions")
    viewTransaction=(action "viewTransaction")
    toaddNewTransaction=(action "addNewTransaction")
}}